# Core build toolchain
build-essential
python3-dev

# For BLAS/LAPACK (SciPy, NumPy)
gfortran
libopenblas-dev
liblapack-dev
libatlas-base-dev

# OpenMP support (HDBSCAN, numba)
libgomp1

# Compression & image libs (wordcloud/Pillow)
zlib1g-dev
libjpeg-dev
libpng-dev
libfreetype6-dev

# pkg-config for finding headers
pkg-config

# Rust toolchain (tiktoken or any Rust‑based wheels)
rustc
cargo
